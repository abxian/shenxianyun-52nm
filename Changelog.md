## v2.5.38

- **Mihomo(Meta) 内核升级至 v1.19.30**

### 卸载与重装

- 修复「提示需要先卸载旧版本，但怎么关进程都提示失败」。安装/卸载时结束后台进程的
  代码有两处缺陷：一是结束进程后没有取回调用结果，导致内部状态被打乱，行为随当时
  恰好有几个进程在跑而变化；二是刚发出结束指令就立刻去删文件，而系统回收进程需要
  时间，文件此刻仍被占用、删除失败，安装程序随后就判定「卸载失败」。现在改为结束
  后确认进程真的退出（最多等 5 秒）再继续。
- 卸载时不再静默留下残留。此前删除失败是不报错的，内核等文件会原地留下，卸载显示
  成功、重装却又提示卸载失败。现在每个文件删除后都会复核，删不掉的会安排在重启后
  清除，并明确告知你需要先重启再装新版本。
- 结束不掉进程时，直接告诉你是哪一个进程，以及下一步该做什么，不再只给一句
  「无法卸载」。应用内静默更新时不会弹窗打断。

### 下载

- GitHub 发布页顶部新增「直接下载」表格，Windows 64 位、macOS Apple 芯片、
  macOS Intel 三个最常用的包可以直接点，下方另有各架构完整列表。此前发布说明既没有
  下载链接，又因流程时序问题被覆盖成一句占位文字，现已一并修复。

## v2.5.37

### TUN 模式

- 修复「安装服务」提示成功、TUN 模式却始终不可用的问题。根因是安装包内打进去的
  系统服务组件与客户端不是同一版本：客户端只认 2.3.0，而构建时下载的一直是上游
  最新版。两者版本号不相等时，客户端一律判定为协议不匹配，服务其实装好了也用不了，
  点「修复服务」还会重装出同一个不匹配的版本，永远修不好。此问题自上游 2026-06-27
  发布新版服务后开始出现，在那之前装好服务的机器不受影响——这正是「以前装过的一直
  能用、现在新装的装不上」的原因。升级本版后会自动换成匹配的服务组件。
- 安装、重装、强制重装完成前增加协议复核。此前只要能连上服务就返回成功，会把一个
  修不好的状态报成「安装成功」，用户只能反复弹管理员授权而毫无进展；现在不匹配就
  如实报错并说明该怎么办。
- 服务诊断与首页自检现在直接显示「已安装服务版本 / 客户端要求版本」。

### 发版流程

- 构建时不再下载上游最新版服务，改为按客户端实际链接的版本下载，并新增发版门禁
  校验四个应用版本号载体与服务版本的一致性。

## v2.5.36

### 发布流程

- 固定版 WebView2 的更新元数据现在必须等安装包和签名全部上传完成后才生成，
  并校验 Windows x64/ARM64 的下载地址与签名齐备，缺任何一项直接失败。
  此前这些情况会静默产出内容残缺的更新元数据，且流水线仍显示成功。

## v2.5.35

### 商业设备身份

- 安全导入使用本机稳定设备键与服务端 canonical device 归一同一台设备；覆盖安装或重装后重新导入不会重复占用物理设备名额。
- 每次重新导入都会撤销旧受管令牌并签发新令牌，避免旧凭据继续占用活动状态。
- 官方配置、托盘与通知统一隐藏提取码和内部配置名，只展示吾爱云品牌订阅。

### 流量与网络恢复

- 受管流量使用持久化累计计数器和幂等序列，服务端确认后才推进本地基线，避免重启或重试造成重复计费。
- 单次上报增加覆盖连接、响应头和响应体的总时限；超时会取消底层请求并用同一序列自动重试。
- 改善启动、断网恢复、节点切换和退出后的调度与系统代理回滚，避免请求卡住或退出后残留代理。

### 导入与发行质量

- 加固安全导入、订阅刷新和节点健康检查，失败时保持原配置，不产生重复官方配置。
- 新增仓库原生 Windows 自动与真机验收链，绑定精确提交、GitHub Actions 构件摘要和测试顺序。
- Development Test 支持续跑同一构建任务，避免重复派发触发并发取消。

## v2.5.34

### 提取码导入可靠性

- 首次导入不再因为所有节点暂时离线或延迟检测失败而拒绝写入，离线环境也可以先保存有效订阅。
- 继续校验订阅格式并以核心实际启动作为最终验证；配置无效或核心启动失败时仍会恢复导入前状态。

### 同步与稳定性

- 启动时刷新受管订阅并同步续费后的到期时间，避免客户端长时间显示旧状态。
- 流量累计改为本地持久化、串行上报、失败退避与随机抖动，降低集中请求并避免重复计费。
- 同步深色模式节点菜单与旧版 macOS WebKit 兼容修复。

## v2.5.33

### 核心可靠性

- 全面加固提取码订阅的导入、切换和更新事务；失败时恢复原活动配置、规则与网络状态，避免留下半成品配置。
- 加固核心单实例、启动就绪、停止超时和服务/Sidecar 状态判断；旧核心清理失败时不再误报已停止。
- “彻底重置”增加分阶段清理与回滚，核心、系统服务或网络清理失败时中止重建并保留可诊断状态，避免重复管理员授权。
- 改善覆盖安装、同目录卸载重装后的旧服务、旧进程、任务计划与配置残留诊断。

### 使用体验

- 切换提取码后，Web 管理页面绑定当前活动配置和控制器，默认打开实时连接页。
- 修复 `api.52nm.de` 直连受 fake-ip 影响时，网页可以访问但客户端服务器检测显示“未连通”的误报；直连失败后会经当前内核或后台代理复测。
- 扩充自检信息，展示关键配置目录、核心 PID、系统服务与计划任务状态，方便定位升级和重置问题。

## v2.5.32

- 修复中文产品名导致 GitHub Release 下载资产前缀被清空的问题，固定使用 `Wuaiyun` ASCII 资产名。
- 修复固定 WebView2 便携包查找错误 Tauri 输出目录的问题。
- 应用界面、安装后名称和节点品牌仍保持“吾爱云”，内部标识与旧版兼容。

## v2.5.31

- 52nm 客户端展示品牌统一更新为“吾爱云”。
- 站点、客户端、节点品牌、安装包名称与发布名称由统一站点配置生成。
- 保留原应用标识、配置目录和 `shenxianyun://` 深链，支持从旧版覆盖升级。
- 新增“删除之前的配置并生成全新配置”，可在系统服务或旧配置不兼容时彻底重建。

## v2.5.30

- “彻底重置”增加删除旧配置并生成全新配置的选项，解决旧配置与新内核协议不兼容时无法启用的问题。
- 重置前可自动创建本地恢复备份，并可同时修复系统服务。
- 重置时保留日志、备份和稳定设备标识，避免同一台设备重复占用设备名额。

## v2.5.29

- 新建公开的 52nm 独立客户端发布线。
- 国内主 API 切换为 `api.52nm.de:5443`，国外备用线路为 `52nm.de` 与
  `www.52nm.de`。
- 运行时网站域名集中配置，保留短票据、受管订阅、设备心跳和流量限制协议。
- 更新通道只指向本仓库 GitHub Release，不覆盖原神仙云 Dufs 或旧客户端。

## v2.5.29-rc.2

### 预览功能

- 提取码和网页一键导入改用短时一次性 ticket，客户端界面与本地配置不再保存或显示真实订阅链接
- 导入后的订阅名称和文件名统一显示当前提取码，避免把后台文件名误认为提取码
- 设备令牌、受管订阅地址和授权信息加密保存在应用私有目录
- 增加 v2 在线心跳、离线通知和幂等流量上报，设备或流量超限时停止代理
- 受管配置允许本地编辑；保存后自动转为本地副本，手动更新时可明确恢复远程管理
- 保留旧订阅链接导入兼容，vpn-web 严格保护开关将在预览真机验证通过后再启用

## v2.5.26

### 修复问题

- 修复客户端无法访问 `api.sxnn.de:5443`，导致提取码、订阅、心跳和流量上报失败
- 提取码导入增加检查、获取订阅、启动联网的可视化流程与失败恢复操作
- 隐藏心跳和订阅请求的底层错误，只显示简明处理提示
- 服务器线路完全自动选择，高级设置只显示汇总连通状态
- 国内主域名固定直连，直连失败时自动切换到可用服务器
- 全局模式改用 `GLOBAL` 节点列表，节点选择会直接更新全局出口
- 同步 Mihomo 插件 0.5.4，改善日志连接与小写配置枚举兼容性

## v2.5.2

### 🐞 修复问题

- macOS 托盘速率可能的样式错误
- 修复订阅 TLS 1.0/1.1 等过旧协议时显示更明确错误原因
- 修复 gzip 压缩订阅响应被当作无效 YAML 导致导入失败的问题
- 修复订阅 URL 使用空密码 Basic Auth 时未发送认证信息的问题
- Linux 托盘可能与其他 tauri 程序托盘冲突导致图标异常
- 修复前端连接页面导致的内存泄漏
- macOS 12(Monterey) 首页 IP 卡兼容性

<details>
<summary><strong> ✨ 新增功能 </strong></summary>

- 增加 TrustTunnel, OpenVPN, Tailscale, GostRelay 节点显示支持

</details>

<details>
<summary><strong> 🚀 优化改进 </strong></summary>

- 关闭 autofill 弹出窗口

</details>
